package org.cura.pages.functions;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.cura.pages.elements.AppointmentConfirmationPageElements;
import org.cura.utils.WebDriverUtility;

public class AppointmentConfirmationPage extends AppointmentConfirmationPageElements {
    static final Logger logger = LogManager.getLogger(AppointmentConfirmationPage.class);
    public WebDriverUtility driverUtility;
}
